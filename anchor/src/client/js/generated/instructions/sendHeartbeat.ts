/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from 'gill';
import { CYBERVAULT_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const SEND_HEARTBEAT_DISCRIMINATOR = new Uint8Array([
  75, 63, 64, 229, 162, 200, 239, 43,
]);

export function getSendHeartbeatDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SEND_HEARTBEAT_DISCRIMINATOR
  );
}

export type SendHeartbeatInstruction<
  TProgram extends string = typeof CYBERVAULT_PROGRAM_ADDRESS,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountVault extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOwner extends string
        ? ReadonlySignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountVault extends string
        ? WritableAccount<TAccountVault>
        : TAccountVault,
      ...TRemainingAccounts,
    ]
  >;

export type SendHeartbeatInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type SendHeartbeatInstructionDataArgs = {};

export function getSendHeartbeatInstructionDataEncoder(): FixedSizeEncoder<SendHeartbeatInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: SEND_HEARTBEAT_DISCRIMINATOR })
  );
}

export function getSendHeartbeatInstructionDataDecoder(): FixedSizeDecoder<SendHeartbeatInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getSendHeartbeatInstructionDataCodec(): FixedSizeCodec<
  SendHeartbeatInstructionDataArgs,
  SendHeartbeatInstructionData
> {
  return combineCodec(
    getSendHeartbeatInstructionDataEncoder(),
    getSendHeartbeatInstructionDataDecoder()
  );
}

export type SendHeartbeatAsyncInput<
  TAccountOwner extends string = string,
  TAccountVault extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  vault?: Address<TAccountVault>;
};

export async function getSendHeartbeatInstructionAsync<
  TAccountOwner extends string,
  TAccountVault extends string,
  TProgramAddress extends Address = typeof CYBERVAULT_PROGRAM_ADDRESS,
>(
  input: SendHeartbeatAsyncInput<TAccountOwner, TAccountVault>,
  config?: { programAddress?: TProgramAddress }
): Promise<
  SendHeartbeatInstruction<TProgramAddress, TAccountOwner, TAccountVault>
> {
  // Program address.
  const programAddress = config?.programAddress ?? CYBERVAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: false },
    vault: { value: input.vault ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.vault.value) {
    accounts.vault.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([99, 121, 98, 101, 114, 95, 118, 97, 117, 108, 116])
        ),
        getAddressEncoder().encode(expectAddress(accounts.owner.value)),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [getAccountMeta(accounts.owner), getAccountMeta(accounts.vault)],
    data: getSendHeartbeatInstructionDataEncoder().encode({}),
    programAddress,
  } as SendHeartbeatInstruction<TProgramAddress, TAccountOwner, TAccountVault>);
}

export type SendHeartbeatInput<
  TAccountOwner extends string = string,
  TAccountVault extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  vault: Address<TAccountVault>;
};

export function getSendHeartbeatInstruction<
  TAccountOwner extends string,
  TAccountVault extends string,
  TProgramAddress extends Address = typeof CYBERVAULT_PROGRAM_ADDRESS,
>(
  input: SendHeartbeatInput<TAccountOwner, TAccountVault>,
  config?: { programAddress?: TProgramAddress }
): SendHeartbeatInstruction<TProgramAddress, TAccountOwner, TAccountVault> {
  // Program address.
  const programAddress = config?.programAddress ?? CYBERVAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: false },
    vault: { value: input.vault ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [getAccountMeta(accounts.owner), getAccountMeta(accounts.vault)],
    data: getSendHeartbeatInstructionDataEncoder().encode({}),
    programAddress,
  } as SendHeartbeatInstruction<TProgramAddress, TAccountOwner, TAccountVault>);
}

export type ParsedSendHeartbeatInstruction<
  TProgram extends string = typeof CYBERVAULT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    owner: TAccountMetas[0];
    vault: TAccountMetas[1];
  };
  data: SendHeartbeatInstructionData;
};

export function parseSendHeartbeatInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedSendHeartbeatInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { owner: getNextAccount(), vault: getNextAccount() },
    data: getSendHeartbeatInstructionDataDecoder().decode(instruction.data),
  };
}
